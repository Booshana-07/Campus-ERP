fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
python-dotenv==1.0.1
requests==2.32.3
python-multipart==0.0.9

# ----- Phase 3A: authentication -----
# Both are optional at runtime: app/security.py falls back to standard-library
# equivalents (PBKDF2-HMAC-SHA256 and a stdlib HS256 implementation) if they are
# missing, so an un-upgraded virtualenv still boots. Installing them is
# recommended — bcrypt is the stronger password KDF of the two.
bcrypt==4.2.0
PyJWT==2.9.0

# ----- Phase 3B -----
# Renders the admin `users.pdf` export. Optional at runtime: without it the
# export endpoint returns a clear 503 and the rest of the platform is unaffected.
reportlab==4.2.5
